fn describe(n: Int) -> String {
  match n {
  | 0 => "zero"
  | 1 => "one"
  | _ => "many"
  }
}

fn main() -> Result<Unit, String> {
  print(describe(0) + " " + describe(1) + " " + describe(42))
  Ok(Unit)
}
